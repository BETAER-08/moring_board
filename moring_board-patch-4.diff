*** Begin Patch
*** Update File: App.tsx
@@
-import * as Calendar from 'expo-calendar';
-import axios from 'axios';
+import * as Calendar from 'expo-calendar';
+import axios from 'axios';
+import { Provider as PaperProvider, MD3LightTheme as DefaultTheme } from 'react-native-paper';
@@
-class LegacyApiService {
-    private readonly WEATHER_API_KEY = '8KL46HqqhxK4T/UAB0bJfFac...';
+class LegacyApiService {
+    private readonly WEATHER_API_KEY = process.env.EXPO_PUBLIC_WEATHER_KEY as string;
@@
-export default function App() {
-  return (
-    <SafeAreaView style={styles.container}>
-      {/* ...existing JSX... */}
-    </SafeAreaView>
-  );
-}
+const theme = { ...DefaultTheme, colors: { ...DefaultTheme.colors, primary: '#2563EB', secondary: '#64748B' } };
+
+export default function App() {
+  return (
+    <PaperProvider theme={theme}>
+      <SafeAreaView style={styles.container}>
+        {/* 기존 JSX 그대로 */}
+      </SafeAreaView>
+    </PaperProvider>
+  );
+}
@@
-// 정류장 렌더 로직 내부: 실시간 없으면 안 보임
+// 정류장 렌더 로직 내부: 실시간 없더라도 노선은 항상 보이기
 // arrivals 를 그대로 사용
+// mergedArrivals: fetchRoutesForStop + fetchAllBusesForStop 병합 결과
*** End Patch